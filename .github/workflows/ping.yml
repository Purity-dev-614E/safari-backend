name: Keep Render Awake

on:
  schedule:
    - cron: "*/5 * * * *"   # run every 5 minutes
  workflow_dispatch:        # allow manual run too

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Call ping endpoint
        run: curl -s https://safari-backend-fgl3.onrender.com/ping
